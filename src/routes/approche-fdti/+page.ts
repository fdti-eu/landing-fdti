import { graphql, type getApproachPageContent$input } from '$houdini';

export function _getApproachPageContentVariables({ params }: any): getApproachPageContent$input {
	return { name: 'French', url: '/' };
}
